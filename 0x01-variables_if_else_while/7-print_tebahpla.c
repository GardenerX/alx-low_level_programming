#include <stdio.h>

/**
 * main - Entry Point
 *
 * description: from z - a
 *
 * Return: Always 0 (success)
 */

int main(void)
{
char m;
m = 'z';

while
(m <= 'z') {
if
(m >= 'a')
putchar(m);
m--;
}
putchar('\n');

return (0);
}
