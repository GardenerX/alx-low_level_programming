#include <stdio.h>

/**
 * main - C program that prints a string/line using printf()
 * Return: 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	Return(0);
}
