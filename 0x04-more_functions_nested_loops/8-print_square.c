#include "main.h"

/**
 * print_square - print square
 * @size:input
 *
 */
void print_square(int size)
{
int h, p;
if
(size <= 0)
{
_putchar('\n');
}
else
{
for (h = 1; h <= size; h++)
{
for (p = 1; p <= size; p++)
{
_putchar(35);
}
_putchar('\n');
}
}
}
