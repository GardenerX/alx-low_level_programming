#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c:input character
 * Return: 1 for true and 0 for false
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
return (1);
else
return (0);
}
