#include "my_math.h"

int add(int a, int b)
{
int sum;
sum = a + b;
return(sum);
}

int sub(int a, int b)
{
return(a - b);
}

int mul(int a, int b)
{
return(a * b);
}

int div(int a, int b)
{
return(a / b);
}

int mod(int a, int b)
{
return(a % b);
}
